# Leave empty to call the same origin the app is served from.
# In development the Vite proxy forwards /api and /media to the Go backend.
# For a split deployment point this at the deployed backend, no trailing slash:
# VITE_API_BASE_URL=https://media-sequencer-api.onrender.com
VITE_API_BASE_URL=
